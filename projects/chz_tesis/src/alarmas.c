/*
 * alarmas.c
 *
 *  Created on: Jun 14, 2017
 *      Author: lchico
 */

/*
#include "alarmas.h"
//#include "sensores.h"
#include "actuadores.h"
#include "stdio.h"


state_t temp_alarm=ON;
state_t battery_alarm=ON;
state_t gps_signal_alarm=ON;

extern float Temperatura;
extern float UmbralTempeMax; // Umbral de temperatura máxima en ºC.
extern float UmbralTempeMin; // Umbral de temperatura mínima en ºC.


void vStartAlarmTask( void ){

	if ( temp_alarm == ON ){
		if ( Temperatura < UmbralTempeMin || Temperatura > UmbralTempeMax){

		}
	}
}
*/
